from django.urls import path
from . import views

app_name = 'appointments'

urlpatterns = [
    # Traditional views (if needed for admin or non-API access)
    # These can be implemented later if required
]
