from django.urls import path
from . import views

app_name = 'appointments'

urlpatterns = [
    # These are placeholder URLs as the main functionality is through the API
    # You can add template-based views here if needed
]